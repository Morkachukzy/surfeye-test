import { SectionLayout } from '@/app/layouts/section';
import { LiveVideos } from './live';

export const HeroSection = () => {
  return (
    <SectionLayout>
      <LiveVideos />
    </SectionLayout>
  );
};
